web: node Backend/server.js
postinstall: npm run install:backend && npm run build:frontend
# If deploying root as a monolith, use scripts in root.
# But for Render separate deploy, Backend/Procfile would just say:
# web: node server.js
